<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EIA.ViewModels"
             x:Class="EIA.Views.AboutPage"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Refresh" Command="{Binding Refresh}"/>
    </ContentPage.ToolbarItems>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <!--Takes user to the search page-->
        <Button Margin="5,5,5,5" Text="Search for items" Font="Bold, 20" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="7"
                        Command="{Binding GoToItems}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" 
                        IsVisible="True"
                        x:Name="findItemButton"/>
        <!--Current Comparison View-->
        <!--Make this formatted better for comparison-->
       
        <Label Grid.Row="1" Grid.Column="0" Text="Item" FontSize="Medium" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center"/>
        <Label Grid.Row="1" Grid.Column="1" Text="Water"/>
        <Label Grid.Row="1" Grid.Column="2" Text="Air"/>
        <Label Grid.Row="1" Grid.Column="3" Text="ESG"/>
        <Label Grid.Row="1" Grid.Column="4" Text="Ecological"/>
        <Label Grid.Row="1" Grid.Column="5" Text="Embedded CO2"/>
        <Label Grid.Row="1" Grid.Column="6" Text="Lifetime CO2"/>
        
        <ListView Grid.Row="2"
            Grid.ColumnSpan="7"
            VerticalOptions="Center"
            HasUnevenRows="True"
            ItemSelected="itemComparison_ItemSelected"
            x:Name="itemComparison">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                             <Label Grid.Row="0" Grid.Column="0" Text="{Binding Text}" FontSize="20" HorizontalTextAlignment="Start" VerticalTextAlignment="Start"/>
                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding WaterScore}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Grid.Row="0" Grid.Column="2" Text="{Binding AirQualityScore}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Grid.Row="0" Grid.Column="3" Text="{Binding CorporateSocialScore}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Grid.Row="0" Grid.Column="4" Text="{Binding EcologyScore}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Grid.Row="0" Grid.Column="5" Text="{Binding EmbeddedEmissionsScore}" FontSize="20" VerticalTextAlignment="Center"/>
                            <Label Grid.Row="0" Grid.Column="6" Text="{Binding LifetimeEmissionsScore}" FontSize="20" VerticalTextAlignment="Center"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</ContentPage>
